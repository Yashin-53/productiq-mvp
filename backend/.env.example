# Optional: wire a real LLM for production-grade extraction (see
# app/services/extraction.py::llm_extract_stub). The MVP runs fully
# offline with deterministic pattern extraction if neither is set.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
